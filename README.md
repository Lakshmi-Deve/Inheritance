# Inheritance
In JavaScript, inheritance is achieved through prototypes, where a child class inherits methods and properties from a parent class. You can create inheritance using the extends keyword in class-based syntax, allowing the subclass to inherit from the parent class.
